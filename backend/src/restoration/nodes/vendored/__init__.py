"""Vendored upstream model code (GPEN, MambaIR) with original licences preserved.

These modules are only imported when the corresponding optional inference
extras are installed and a node actually runs — never at app startup.
"""
