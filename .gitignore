# graphify output - regenerate locally with `/graphify .` (see docs/GRAPHIFY_WORKFLOW.md)
graphify-out/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/

# PyInstaller generates this from backend/packaging/build_exe.py, which is the
# real source of the desktop build — the .spec is a regenerated artifact.
*.spec

# Test & tooling caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# Model weights are never committed: multi-gigabyte, checksum-pinned in each
# node's weight manifest, and fetched on first use.
weights/
